# Target-Approximating Path Analysis - Python Dependencies
# 권장: Python 3.10 이상
#
# 설치:
#   pip install -r requirements.txt
#
# 참고: tkinter는 Python 표준 라이브러리이나, 일부 Linux 배포판에서는
#       별도 설치가 필요할 수 있습니다 (Debian/Ubuntu: sudo apt install python3-tk).

# 그래프 이론 - 경로 탐색 (Yen's K-shortest, Dijkstra 등)
networkx>=3.0

# 지리공간 벡터 데이터 처리
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyproj>=3.4

# 래스터(DEM) 처리 - 표면거리 계산
rasterio>=1.3

# 수치 연산 / 데이터프레임
numpy>=1.24
pandas>=2.0
